/**
 * @param {string} s
 * @return {string[]}
 */
var restoreIpAddresses = function(s) {
  let temporaryArr = []
  if (s.length < 4 || s.length > 3000 || !Number.isInteger(Number(s))) {
    return []
  }

  for (let i = 0; i < s.length; i++) {
    const s = s[i]
  }
};
